#include <iostream>

class Node{
    Node first = NULL;
    Node last = NULL;
public:
    int value;
    void addNode(Node n) {
        if (n.value < )
    }
};

int main(int argc, char *argv[]) {

    return 0;
}